server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Serve built assets
    location / {
        try_files $uri /index.html;
    }

    # (Optional) If you later reverse-proxy API here, we can add /api block.
}
